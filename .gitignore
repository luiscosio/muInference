# Build outputs.
mucore/build/

# Model inputs are fetched and hash-verified, not committed.
# See model/fetch.sh and model/SHA256SUMS.
model/*.bin

# Logits dumps produced by the test suites.
*.logits

# Vendored Microkit SDK (69 MB, fetched by vendor/microkit/fetch.sh).
vendor/microkit/microkit-sdk-*/
